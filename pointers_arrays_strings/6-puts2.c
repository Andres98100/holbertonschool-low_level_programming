#include<stdio.h>
#include"main.h"
/**
*puts2 - prints to stdout
*@str: variable
*/
void puts2(char *str)
{
	int i;
	int j;
	while (c[i] != '\0')
	{
		i++;
	}
	for (j = 0; j < i; j += 2)
	{
		putchar(c[j]);
	}
	putchar('\n');
}
