#include <stdio.h>
#include <stdlib.h>
#include "dog.h"
dog_t *new_dog(char *name, float age, char *owner)
{
	struct dog_t dog_t;

	if (dog_t == NULL)
		return (NULL);
	dog_t = malloc(dog_t * sizeof(char));
	if (save != NULL)
	{
		dog_t>name = name;
		dog_t>age = age;
		dog_t->owner = owner;
	}
	if (dog_t->name == name)
	{
		dog_T = name;
	}
	return (dog_t);
}
